const primeiroNome = "Mario";
const sobrenome = "Bros";
const apelido = "";

if (primeiroNome) {
    if (apelido) {
        console.log(apelido)
    } else {
        console.log(primeiroNome + ' ' + sobrenome)
    }
}
