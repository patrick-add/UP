const disjuntores = [false, true, true, false, false, true, false, false, true, true];

for (let verificador = 0; verificador < disjuntores.length; verificador++) {
    if (disjuntores[verificador]) {
        console.log(verificador)
    }
}

